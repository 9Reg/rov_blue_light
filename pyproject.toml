[project]
name = "rov-brain"
version = "0.1.0"
description = "AI-enabled underwater ROV control system"
requires-python = ">=3.11"

dependencies = [
  "opencv-python",
  "numpy",
  "fastapi",
  "uvicorn",
  "pyserial"
]

[tool.ruff]
line-length = 100